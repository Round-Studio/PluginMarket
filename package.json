{
  "name": "plugin-market-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:index": "node scripts/generate-plugin-index.mjs",
    "dev": "npm run generate:index && wrangler pages dev public --compatibility-date=2026-05-04",
    "deploy": "npm run generate:index && wrangler pages deploy public",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
